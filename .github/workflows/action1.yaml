name: Testing repo
on: [push/pull]

jobs:
        bananas:
                name: 'Checkout'
                runs-on: ubuntu-latest
                steps:
                        - name: 'Checkout'
                          uses: actions/checkout@v3
                          with:
                                  ref: #GITHUB_SHA

                        - name: 'Print Something'
                          run: echo $(derd.txt)

